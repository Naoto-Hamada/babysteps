  <div class="contents row" >
    <p><%= @nickname %>さんのこれまでのベイビーステップ</p>
    <% @tweets.each do |tweet| %>
      <img class="content_post" src="<%= tweet.image %>"></img>
      <p><%= tweet.text %></p>
    <% end %>
    <%= paginate(@tweets) %>
  </div>